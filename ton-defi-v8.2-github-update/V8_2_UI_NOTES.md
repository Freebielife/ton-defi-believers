# v8.2 — visual and navigation update

- User-provided TON DeFi Believers smile logo added to the header, hero and favicon.
- Local protocol artwork added for all catalog protocols; no external image requests are required at runtime.
- The protocol name and icon are now the direct project link.
- The redundant far-right action column was removed.
- Background and surfaces use a lightweight cyan–violet–mint gradient palette.
- Category accents help distinguish GRAM, stablecoin, GRAM–USDT, BTC and ETH sections.
- Referral and tracking parameters are not included in public links.
- `.github/workflows/update-data.yml` checks for updated data once per hour at minute 17.
- The site remains static and works without a server or database.
