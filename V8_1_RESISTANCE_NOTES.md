# v8.1 Resistance-ready

- Публичный сайт использует только `public/data/market-catalog.json`.
- Тяжёлые служебные JSON остаются в `data/` для сборщика, но не публикуются.
- В интерфейсе автоматически показывается возраст снимка: до 24 часов — актуальный, 24–48 часов — предупреждение, более 48 часов — устаревший.
- Реферальные параметры удалены.
- Для Resistance Tools выбирается распакованная папка, где `index.html` находится в корне.
