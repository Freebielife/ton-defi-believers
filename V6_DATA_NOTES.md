# v6: STON.fi и DeDust

Версия v6 меняет только получение данных двух основных DEX.

## STON.fi

- Пул `USDe–tsUSDe` закреплён по адресу `EQAF6mNbKhaMrfyhdNcrEnRKW1fXA3jmkS6KM7azm9PunYx5`.
- Используется точечный запрос `/v1/pools/{address}`.
- Основная доходность: `apy_7d`; запасные варианты: `apy_30d`, затем `apy_1d`.
- TVL: `lp_total_supply_usd`.
- Резервы сверяются с `get_pool_data`; для пула v2 используются позиции 3 и 4.

## DeDust

- Пилот отслеживает один понятный пул TON/USDT по адресу `EQA-X_yo3fzzbDbJ_0bzFWKqtRuZFIRa1sJsveZJ1YpViO3r`.
- Активы фиксируются адресами и конфигурацией; значение `jetton` не используется как название токена.
- TVL считается по резервам TON и USDT и цене пула.
- Fee APR считается за полное окно 7 дней:

  `объём × комиссия пула × 80% ÷ TVL × 365 / 7`

- Резервы проверяются через `get_reserves`, комиссия — через `get_trade_fee`, когда TON RPC доступен.

## Поведение при ошибке

- API-запросы повторяются до трёх раз.
- Ошибка STON.fi или DeDust не удаляет предыдущий успешный снимок.
- Неполное 7-дневное окно DeDust не публикуется как новая ставка.
- На сайте видны тип показателя, период и время обновления.
